---
description: "Code review for Spring Boot projects: verify all instruction file rules are followed."
---

Review the files that were changed in the current context. If the user explicitly requests a full project review, review all source files.

For each area below, verify that all rules in the referenced instruction file are followed. Report findings grouped by file; label each finding with the specific rule it violates and the instruction file it comes from. Skip areas that have no relevant files in scope.

## Instruction Files
`spring-boot-*.instructions.md`
